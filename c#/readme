Эти исходники - часть плагина к Kerbal Space Program (это автопилот).

Он построен на клиент-серверной модели (по крайней мере Debug-версия, перезапуск игры занимает слишком долго для итерации разработки,
перезагрузка сборок через AppDomain'ы не работает - баг Unity).

1. net

Библиотека для работы с сетью. Ориентирована на скорость сереализации/десериализации данных
(бывает нужно гонять данные туда-сюда по 100 раз в секунду) и минимизацию траффика.

Протокол основан на бинарном представлении данных (для скорости) + теги типов (для поиска ошибок).
Сериализация произвольных объектов возможна через наследование от базового класса (Serializable) + задание с помощью тега
уникального идентефикатора (символьного), из которого в run-tim'е генерируется численный идентефикатор, используемый протоколом. 

Поверх протокола работают реализации клиента и сервера (есть базовые классы + реализация обмена данными, специфичная для этого проекта)

2. gui

Реализация сетевого графического интерфейса. Может служить примером использования библиотеки для работы с сетью. 
Здесь представлен набор классов графического интерфейса (такой вариант кажется мне более удобным и расширяемым,
чем предоставляемые Unity методы, также он хорошо подходит для сетевого взаимодействия).


